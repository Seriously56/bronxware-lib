local library = loadstring(game:HttpGet("YOUR_RAW_SCRIPT_URL_HERE"))()

local window = library:CreateWindow("My Script", Vector2.new(600, 500), Enum.KeyCode.RightShift)
local watermark = library:CreateWatermark("{game} | {fps}", Vector2.new(10, 10))

local mainTab = window:CreateTab("Main")
local leftSector = mainTab:CreateSector("Controls", "left")
local rightSector = mainTab:CreateSector("Settings", "right")

-- Add elements
leftSector:AddButton("Test Button", function()
    print("Button pressed!")
end)

local toggle = leftSector:AddToggle("ESP", false, function(state)
    print("ESP:", state)
end)

leftSector:AddSlider("Walk Speed", 16, 16, 100, 1, function(value)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
end)

rightSector:AddDropdown("Gun", {"AK-47", "M4A1", "Shotgun"}, "AK-47", false, function(selected)
    print("Selected gun:", selected)
end)

-- Add config system
mainTab:CreateConfigSystem("right")
